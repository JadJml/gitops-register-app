apiVersion: v1
kind: Service
metadata:
  name: register-app-service
spec:
  type: NodePort
  selector:
    app: register-app
  ports:
    - port: 80        # port du service
      targetPort: 8080  # port sur le pod
      nodePort: 30080
